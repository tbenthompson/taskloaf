# Enable C++ support
language: cpp
sudo: true
dist: trusty
python: 
  - "2.7"
  - "3.5"
matrix:
  include:
    - compiler: gcc
      before_install:
      - ./dev/install_travis_gcc
    - compiler: gcc
      before_install:
      - ./dev/install_travis_gcc
      - sudo apt-get install -qq libopenmpi-dev openmpi-bin
    - compiler: clang
      before_install:
      - ./dev/install_travis_gcc
    - compiler: clang
      before_install:
      - ./dev/install_travis_gcc
      - sudo apt-get install -qq libopenmpi-dev openmpi-bin
script:
  - ./dev/travis_cpp_test
  - ./dev/travis_py_test
